# 版本管理系统 - 使用指南

## 📋 概述
这套版本管理系统帮助您自动记录美术资源管理工具的每次更新，包括新功能、优化改进和问题修复。

## 📁 相关文件
- `CHANGELOG.md` - 主要的版本更新日志文档
- `update_changelog.py` - 自动更新版本日志的Python工具
- `更新日志.bat` - Windows批处理文件，提供可视化界面
- `VERSION_GUIDE.md` - 本使用指南

## 🚀 快速使用

### 方法一：使用批处理文件（推荐）
1. 双击 `更新日志.bat`
2. 选择 `[1] 交互式更新版本日志`
3. 按提示输入更新内容
4. 系统自动生成并插入新版本记录

### 方法二：直接使用Python脚本
```bash
# 交互式更新
python update_changelog.py

# 查看帮助
python update_changelog.py --help
```

## 📝 使用流程

### 1. 启动更新工具
运行工具后会显示：
```
🚀 美术资源管理工具 - 版本日志更新器
==================================================
版本号 (默认: v0.0.3): 
```

### 2. 输入版本号
- 直接按回车：使用自动递增的版本号
- 手动输入：如 `v1.0.0`（重大版本更新）

### 3. 添加更新内容
按类别依次输入更新内容：

#### 🆕 新增功能
```
🆕 新增功能:
  - Avatar文件包检测功能
  - 智能GUID对比系统
  - （空行结束输入）
```

#### 🔧 优化改进  
```
🔧 优化改进:
  - 改进检测报告的用户体验
  - 优化错误信息显示方式
  - （空行结束输入）
```

#### 🐛 问题修复
```
🐛 问题修复:
  - 修复了误报正常文件的问题
  - 解决GUID解析异常
  - （空行结束输入）
```

### 4. 自动生成记录
工具会自动：
- 插入新版本到CHANGELOG.md顶部
- 添加当前日期
- 格式化内容
- 保持文档结构

## 📖 版本号规则

### 语义化版本控制
采用 `v主版本.次版本.修订版本` 格式：

- **主版本号（Major）**：不兼容的API修改
  - 例：v1.0.0 → v2.0.0
  - 使用场景：重大架构变更、界面大改版

- **次版本号（Minor）**：向后兼容的功能性新增
  - 例：v1.0.0 → v1.1.0
  - 使用场景：新增重要功能、新的检测类型

- **修订版本号（Patch）**：向后兼容的问题修正
  - 例：v1.0.0 → v1.0.1
  - 使用场景：bug修复、小优化、文档更新

### 自动递增规则
- 默认：自动递增修订版本号（最常用）
- 手动：根据更新重要性手动指定

## 📅 更新记录格式

生成的更新记录格式如下：
```markdown
## v0.0.3 - 2024年12月20日

### 🆕 新增功能
- Avatar文件包all.filelist检测功能
- 智能GUID格式错误识别

### 🔧 优化改进  
- 改进了检测报告的可读性
- 优化了错误信息的展示方式

### 🐛 问题修复
- 修复了误报正常文件为错误的问题
- 解决了GUID解析时的异常处理

---
```

## 🎯 最佳实践

### 1. 及时记录
每次完成功能开发或修复bug后，立即更新版本日志。

### 2. 详细描述
- ✅ 好的描述：`添加Avatar文件包all.filelist完整性检测功能`
- ❌ 不好的描述：`修复bug`、`优化代码`

### 3. 用户视角
从美术同学的角度描述更新内容，说明对他们的影响。

### 4. 分类准确
- 新功能：之前没有的功能
- 优化改进：现有功能的增强
- 问题修复：修复已知问题

### 5. 同步版本号
更新日志后，记得同步更新主程序中的版本号：
```python
self.setWindowTitle("美术资源管理工具 v0.0.3")  # 保持一致
```

## 🔍 查看历史
可以通过以下方式查看更新历史：
1. 直接打开 `CHANGELOG.md` 文件
2. 运行 `更新日志.bat` 选择查看日志
3. Git提交记录（如果使用Git管理）

## 📞 支持
如果在使用过程中遇到问题：
1. 检查Python环境是否正确安装
2. 确认脚本文件是否完整
3. 查看错误信息进行排查

---
*最后更新：2024年12月20日* 