# 🧪 热更新功能测试指南

## 📋 测试准备

### 1. 启动测试服务器
在项目目录下运行测试服务器：
```bash
python test_update_server.py
```

您会看到：
```
🚀 启动测试热更新服务器...
📡 服务器地址: http://localhost:8000
📋 可用接口:
  GET  http://localhost:8000/api/check_update?current_version=1.0.0
  GET  http://localhost:8000/api/download/v1.1.0/art_resource_manager.py
  GET  http://localhost:8000/api/version_info

✅ 服务器已启动，等待请求...
```

### 2. 启动美术资源上传工具
在另一个终端窗口运行：
```bash
python art_resource_manager.py
```

或者直接运行打包好的exe：
```bash
美术资源上传工具.exe
```

## 🧪 测试步骤

### **第一步：检查热更新功能是否启用**

1. 启动工具后，查看控制台输出
2. 应该看到：`✅ 热更新功能已启用`
3. 在工具界面中，点击菜单栏 **"帮助"**
4. 确认看到 **"检查更新"** 选项

### **第二步：测试检查更新功能**

1. 点击菜单栏 **"帮助" → "检查更新"**
2. 状态栏应显示：`正在检查更新...`
3. 几秒后应弹出更新对话框，显示：
   - 🎉 **发现新版本 1.1.0！**
   - **当前版本**: 1.0.0
   - **更新说明**: 详细的更新内容
   - **立即更新** 和 **稍后提醒** 按钮

### **第三步：测试更新对话框**

1. 在更新对话框中查看更新内容
2. 确认显示的信息正确：
   - 版本号：1.0.0 → 1.1.0
   - 更新说明：包含新功能和改进
   - 更新文件：1个文件

### **第四步：测试更新流程（可选）**

⚠️ **注意**: 这会实际执行更新，请谨慎操作

1. 点击 **"立即更新"** 按钮
2. 应显示进度对话框：`正在下载更新...`
3. 进度条会显示下载进度
4. 完成后会询问是否重启应用

### **第五步：测试无更新情况**

1. 修改测试服务器中的版本号（将 `latest_version = "1.1.0"` 改为 `"1.0.0"`）
2. 重启测试服务器
3. 再次点击 **"检查更新"**
4. 应显示：`当前已是最新版本！`

## 🔍 预期结果

### **✅ 成功指标**

1. **菜单显示正常**
   - 帮助菜单中有"检查更新"选项
   - 点击后不报错

2. **网络连接正常**
   - 能连接到测试服务器
   - 状态栏显示"正在检查更新..."

3. **更新对话框正常**
   - 弹出美观的更新对话框
   - 显示正确的版本信息和更新说明
   - 按钮功能正常

4. **服务器响应正常**
   - 测试服务器控制台显示收到请求
   - 返回正确的JSON响应

### **❌ 可能的问题**

1. **"热更新功能不可用"**
   - 检查 `hot_update_manager.py` 是否存在
   - 确认相关依赖已安装

2. **网络连接失败**
   - 确认测试服务器正在运行
   - 检查端口8000是否被占用

3. **更新对话框不显示**
   - 检查控制台错误信息
   - 确认PyQt5组件正常

## 📊 测试服务器日志

正常的服务器日志应该显示：
```
📡 收到请求: /api/check_update
🔍 检查更新请求 - 当前版本: 1.0.0
✅ 返回更新信息: 版本 1.1.0
🌐 15:30:45 - "GET /api/check_update?current_version=1.0.0 HTTP/1.1" 200 -
```

## 🛠️ 调试技巧

### **查看详细日志**
在工具运行时观察控制台输出，包括：
- 热更新初始化信息
- 网络请求状态
- 错误信息

### **手动测试API**
可以在浏览器中直接访问：
- http://localhost:8000/api/version_info
- http://localhost:8000/api/check_update?current_version=1.0.0

### **修改测试数据**
编辑 `test_update_server.py` 中的：
- `latest_version` - 改变版本号
- `description` - 修改更新说明
- `has_update` - 控制是否有更新

## 🎉 测试完成

如果所有步骤都正常，说明热更新功能已成功集成！

接下来您可以：
1. 重新打包exe（包含修复后的热更新功能）
2. 搭建正式的更新服务器
3. 配置正式的更新服务器地址
4. 分发给用户使用

---
**测试服务器**: `test_update_server.py`  
**工具版本**: 1.0.0  
**测试日期**: 2025年9月2日
