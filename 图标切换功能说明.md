# 🎯 美术资源管理工具 - 动态图标切换功能

## 📋 功能概述

工具现在支持根据检查结果自动切换窗口图标，提供直观的状态反馈：

- **正常状态**：使用 `app_icon_lv.ico`（绿色图标）
- **错误状态**：使用 `app_icon.ico`（红色图标）

## 🔄 图标切换时机

### 1. 程序启动
- 初始化时设置为正常状态图标（`app_icon_lv.ico`）

### 2. 检查完成
根据资源检查结果自动切换：
- ✅ **检查通过**：切换到正常状态图标
- ❌ **检查失败**：切换到错误状态图标

### 3. 推送操作完成
- ✅ **推送成功**：切换到正常状态图标

### 4. 维护操作完成
- ✅ **清除GUID缓存成功**：切换到正常状态图标

### 5. 文件列表管理 🆕
- ✅ **清空文件列表**：切换到正常状态图标

### 6. Git分支操作完成 🆕
- ✅ **分支切换成功**：切换到正常状态图标
- ✅ **拉取分支成功**：切换到正常状态图标
- ✅ **重置仓库成功**：切换到正常状态图标

## 💡 图标说明

| 图标文件 | 状态 | 说明 |
|---------|------|------|
| `app_icon_lv.ico` | 正常状态 | 绿色图标，表示工具处于正常状态 |
| `app_icon.ico` | 错误状态 | 红色图标，表示存在需要处理的问题 |

## 🛠️ 技术实现

### 核心方法

#### `_get_icon_path(icon_name: str) -> str`
获取图标文件路径，支持开发环境和打包环境：
- **开发环境**：从项目目录加载图标
- **打包环境**：从 `sys._MEIPASS` 加载图标

#### `_update_window_icon(has_errors: bool = False)`
动态更新窗口图标：
- `has_errors=False`：使用正常状态图标
- `has_errors=True`：使用错误状态图标

### 备用机制
- 如果目标图标不存在，会自动尝试使用另一个图标作为备用
- 确保至少有一个图标文件时，功能不会失效

### 错误处理
- 图标加载异常时，会在控制台和日志中记录错误信息
- 不会影响工具的正常功能

## 📝 日志输出

图标切换操作会在日志中显示明确的状态信息：
- `🟢 切换到正常状态图标` - 切换到正常状态
- `🔴 切换到错误状态图标` - 切换到错误状态
- `⚠️ 图标文件不存在，使用备用图标` - 备用机制激活

## 🔍 测试验证

运行 `test_icon_switch.py` 脚本可以：
1. 检查图标文件是否存在
2. 验证功能是否准备就绪
3. 查看详细的使用说明

```bash
python test_icon_switch.py
```

## 🎨 自定义图标

如需使用自定义图标：

1. **替换图标文件**：
   - 保持文件名不变：`app_icon.ico` 和 `app_icon_lv.ico`
   - 推荐尺寸：32x32 或 48x48 像素
   - 格式：.ico 文件

2. **重新打包**：
   - 开发环境：直接替换文件即可
   - 生产环境：需要重新运行打包脚本

## 🆕 最新优化

### 智能状态还原
现在工具会在更多成功操作后自动将图标还原为正常状态：

- **清空文件列表**：点击清空按钮后，图标恢复绿色，表示回到初始状态
- **Git分支操作**：分支切换、拉取更新、重置仓库成功后，图标恢复绿色
- **操作日志**：每次图标切换都会在日志中显示，提供清晰的状态反馈

### 用户体验提升
- **状态更一致**：任何清理或重置操作都会重置图标状态
- **反馈更及时**：用户操作后立即看到状态变化
- **逻辑更清晰**：成功=绿色，失败=红色，简单直观

## ✅ 功能优势

1. **直观反馈**：用户可以通过任务栏图标快速了解工具状态
2. **自动切换**：无需手动操作，根据工具状态自动更新
3. **兼容性好**：支持开发环境和打包后的生产环境
4. **备用机制**：即使图标文件缺失也能保证基本功能
5. **统一体验**：所有相关操作都会触发合适的图标状态
6. **智能状态管理**🆕：任何成功的操作都会自动还原为正常状态
7. **操作反馈及时**🆕：日常操作如清空列表也有相应的状态反馈
8. **全面覆盖**🆕：Git操作、文件管理等各种场景都有图标状态支持 