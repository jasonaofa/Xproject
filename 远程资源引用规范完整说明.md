# 🌐 远程资源引用规范完整说明

## 📖 规范定义

### 🎯 核心原则
**禁止本地资源引用远程资源，允许远程资源之间相互引用**

### 📂 目录分类定义

#### **远程资源目录**：
- `Assets/remotes/entity/` 及其所有子目录
- 包含所有商业角色、共享资源等

#### **本地资源目录**：
- **除** `Assets/remotes/entity/` **之外的所有目录**
- 包括但不限于：
  - `Assets/minigame/entity/Environment/Scenes/`
  - `Assets/minigame/entity/Character/`
  - `Assets/avatar/`
  - `Assets/weapon/`
  - `Assets/effect/`
  - `Assets/ui/`
  - 等等...

## 🚫 禁止的引用关系

### ❌ **严格禁止**：本地资源 → 远程资源
```
示例违规情况：
引用文件：Assets/minigame/entity/Environment/Scenes/140502/scene.prefab
被引用文件：Assets/remotes/entity/character/business_character.prefab
结果：❌ 应该报错并阻止上传
```

### ❌ **具体禁止场景**：
1. **minigame项目引用商业角色**：
   - `Assets/minigame/entity/Environment/Scenes/140502/*` → `Assets/remotes/entity/character/*`
   - `Assets/minigame/entity/Character/*` → `Assets/remotes/entity/character/*`

2. **avatar项目引用远程资源**：
   - `Assets/avatar/140001/*` → `Assets/remotes/entity/materials/*`

3. **weapon项目引用远程资源**：
   - `Assets/weapon/sword_001/*` → `Assets/remotes/entity/effects/*`

4. **UI项目引用远程资源**：
   - `Assets/ui/main_menu/*` → `Assets/remotes/entity/textures/*`

## ✅ 允许的引用关系

### ✅ **完全允许**：远程资源 → 远程资源
```
示例合规情况：
引用文件：Assets/remotes/entity/character/hero_001.prefab
被引用文件：Assets/remotes/entity/materials/hero_skin.mat
结果：✅ 允许，不报错
```

### ✅ **完全允许**：本地资源 → 本地资源
```
示例合规情况：
引用文件：Assets/minigame/entity/Environment/Scenes/140502/scene.prefab
被引用文件：Assets/minigame/entity/Environment/Materials/ground.mat
结果：✅ 允许，不报错
```

## 🔧 当前实现问题分析

### 🐛 **发现的Bug**：

#### **问题1：路径检查不完整**
```python
# 当前代码问题
if 'assets/remotes/entity' in referencing_file_normalized or '/remotes/entity' in referencing_file_normalized:
    return None  # 允许引用

# 问题：这个检查条件不够严格
# minigame/entity/Environment/Scenes/140502/ 不包含 'remotes/entity'
# 所以被错误地认为是"本地资源引用远程资源"应该报错
# 但实际上这确实是本地资源引用远程资源，应该报错！
```

#### **问题2：逻辑判断错误**
当前逻辑实际上是**正确的**，但可能存在其他问题：
1. **GUID检查范围**：可能某些文件类型没有被检查
2. **Git扫描范围**：可能远程资源没有被正确扫描到Git仓库中
3. **路径标准化**：可能路径处理有问题

## 🧪 需要验证的场景

### **测试用例1：minigame引用remotes（应报错）**
```
引用文件：Assets/minigame/entity/Environment/Scenes/140502/scene.prefab
被引用文件：Assets/remotes/entity/character/business_character.prefab
预期结果：🔴 报错 - "禁止引用远程资源"
```

### **测试用例2：remotes引用remotes（应允许）**
```
引用文件：Assets/remotes/entity/character/hero.prefab
被引用文件：Assets/remotes/entity/materials/skin.mat
预期结果：✅ 允许 - 远程资源间引用
```

### **测试用例3：minigame引用minigame（应允许）**
```
引用文件：Assets/minigame/entity/Environment/Scenes/140502/scene.prefab
被引用文件：Assets/minigame/entity/Environment/Materials/ground.mat
预期结果：✅ 允许 - 本地资源间引用
```

## 🔍 排查步骤

### **步骤1：确认文件检查范围**
- 检查 `140502` 目录下的文件是否被包含在检查列表中
- 确认文件类型是否在需要检查GUID的范围内

### **步骤2：确认Git扫描范围**
- 验证远程资源是否被正确扫描到 `git_guids_dict` 中
- 检查被引用的商业角色文件GUID是否存在于Git仓库映射中

### **步骤3：确认路径处理**
- 验证路径标准化是否正确
- 检查相对路径和绝对路径的处理逻辑

### **步骤4：确认检查调用**
- 验证 `_check_remote_resource_reference` 方法是否被正确调用
- 检查是否有异常被捕获但没有正确处理

## 💡 解决方案建议

### **方案1：增强日志输出**
在检查过程中增加详细日志，帮助定位问题：
```python
self.status_updated.emit(f"🔍 检查远程引用: {target_guid}")
self.status_updated.emit(f"   引用文件: {referencing_file}")
self.status_updated.emit(f"   目标路径: {relative_resource_path}")
```

### **方案2：完善路径检查逻辑**
确保路径检查逻辑覆盖所有可能的路径格式

### **方案3：扩展检查范围**
确保所有相关文件类型都被包含在检查范围内

## 📝 规范总结

1. **明确定义**：`Assets/remotes/entity/` = 远程，其他 = 本地
2. **严格禁止**：本地 → 远程
3. **完全允许**：远程 → 远程，本地 → 本地
4. **检查时机**：GUID引用检查阶段
5. **错误级别**：严重错误，阻止上传
6. **解决方案**：复制远程资源到本地，或移除引用关系

---

**创建时间**: 2025-09-12
**适用版本**: v1.0.19+
**规范状态**: 待完善实现





