# 🚀 最简单的热更新部署方案

## 🎯 **推荐方案：使用现有Git服务器**

基于您提到的Git地址，我推荐使用您现有的GitLab服务器来实现热更新。

### 📋 **方案优势**
- ✅ **无需额外服务器** - 使用现有Git基础设施
- ✅ **零成本** - 不需要申请新服务器
- ✅ **安全可靠** - 基于现有的权限管理
- ✅ **简单维护** - 就像管理代码一样管理更新

## 🛠️ **具体实施步骤**

### **第一步：在Git上创建更新项目**

在您的GitLab服务器上创建一个新项目：
```
项目地址: http://client_gitlab.miniworldplus.com:83/miniwan/art-tool-updates
项目名称: art-tool-updates
访问权限: 内部项目（团队可访问）
```

### **第二步：创建更新文件结构**

在项目中创建以下目录结构：
```
art-tool-updates/
├── api/
│   ├── check_update.json      # 版本检查API
│   └── version_info.json      # 版本信息API
├── files/
│   └── v1.1.0/
│       └── art_resource_manager.py  # 更新文件
└── README.md                  # 说明文档
```

### **第三步：配置API响应文件**

**api/check_update.json** (有更新时的响应):
```json
{
  "has_update": true,
  "latest_version": "1.1.0",
  "current_version": "1.0.0",
  "description": "🎉 发现新版本 1.1.0！\n\n📋 更新内容:\n• 🔥 新增热更新功能\n• 🚨 修复远程资源引用检查\n• 🌐 支持Avatar/MiniUniverse子目录\n\n建议立即更新以获得最佳体验！",
  "files": [
    {
      "path": "art_resource_manager.py",
      "url": "http://client_gitlab.miniworldplus.com:83/miniwan/art-tool-updates/raw/main/files/v1.1.0/art_resource_manager.py",
      "hash": "",
      "size": 650000
    }
  ],
  "mandatory": false,
  "release_date": "2025-09-02T10:30:00",
  "min_supported_version": "1.0.0"
}
```

**api/version_info.json** (版本信息):
```json
{
  "current_version": "1.0.0",
  "latest_version": "1.1.0",
  "server_time": "2025-09-02T10:30:00",
  "available_versions": ["1.0.0", "1.1.0"],
  "server_status": "active"
}
```

### **第四步：修改工具配置**

将工具中的服务器地址修改为：
```python
# 在 art_resource_manager.py 中修改
self.hot_updater = HotUpdateManager(
    current_version="1.0.0",
    update_server_url="http://client_gitlab.miniworldplus.com:83/miniwan/art-tool-updates/raw/main/api"
)
```

## 🔄 **日常更新流程**

### **发布新版本时：**

1. **更新代码** - 修改 `art_resource_manager.py`
2. **上传文件** - 将新版本文件上传到 `files/v1.2.0/` 目录
3. **更新API** - 修改 `api/check_update.json` 中的版本信息
4. **推送到Git** - `git push` 完成发布

### **用户更新时：**
1. 用户点击"帮助 → 检查更新"
2. 工具自动连接Git服务器检查版本
3. 发现新版本后弹出更新对话框
4. 用户确认后自动下载和应用更新

## 🌐 **其他简单方案**

### **方案二：局域网HTTP服务器**
如果团队都在同一局域网：
```bash
# 在一台固定IP的电脑上运行
python test_update_server.py
# 其他人连接到: http://192.168.1.100:8000/api
```

### **方案三：免费云服务**
- **GitHub Pages**: 免费托管静态文件
- **Netlify**: 免费额度，支持API函数
- **Gitee Pages**: 国内访问更快

## 🎯 **推荐配置**

基于您的情况，我推荐使用**方案一（Git服务器）**：

```python
# 最终配置
UPDATE_SERVER_URL = "http://client_gitlab.miniworldplus.com:83/miniwan/art-tool-updates/raw/main/api"

# 或者如果需要更简单的测试
UPDATE_SERVER_URL = "http://localhost:8000/api"  # 本地测试
```

## 📝 **实施建议**

1. **先测试本地** - 使用我们的 `test_update_server.py` 验证功能
2. **再部署Git** - 确认无误后迁移到Git服务器
3. **逐步推广** - 先给少数用户测试，再全面推广

这样您就不需要申请任何新服务器，直接使用现有的Git基础设施即可！

---
**总结**: 使用现有Git服务器是最经济、最简单的热更新方案！





