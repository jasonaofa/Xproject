# 美术资源热更新服务器开机启动设置指南

## 📋 概述

为了方便美术同学随时获取最新版本，您可以设置热更新服务器在开机时自动启动。本指南提供了多种设置方式。

## 🚀 快速设置（推荐）

### 方式1：一键设置
1. 双击运行 `一键设置开机启动.bat`
2. 按照提示完成设置
3. 重启电脑测试

### 方式2：手动设置
1. 双击运行 `设置开机启动.py`
2. 选择 `[2] 设置开机启动（注册表方式，推荐）`
3. 重启电脑测试

## 📁 文件说明

### 启动脚本
- `启动热更新服务器.bat` - 普通启动（显示窗口）
- `后台启动热更新服务器.bat` - 后台启动（推荐）
- `热更新服务器管理.bat` - 服务器管理工具

### 设置工具
- `设置开机启动.py` - Python设置工具（功能最全）
- `一键设置开机启动.bat` - 批处理一键设置（最简单）

## 🔧 设置方式对比

| 方式 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| 启动文件夹 | 简单易用，用户可见 | 可能被用户误删 | ⭐⭐⭐ |
| 注册表 | 稳定可靠，不易误删 | 需要管理员权限 | ⭐⭐⭐⭐⭐ |
| 任务计划程序 | 功能强大，可定时 | 设置复杂 | ⭐⭐⭐⭐ |

## 📊 检查启动状态

### 方法1：使用管理工具
1. 运行 `热更新服务器管理.bat`
2. 选择 `[4] 查看服务器状态`

### 方法2：使用设置工具
1. 运行 `设置开机启动.py`
2. 选择 `[4] 检查启动状态`

### 方法3：手动检查
1. 按 `Win + R`，输入 `shell:startup`，打开启动文件夹
2. 查看是否有 `美术资源热更新服务器.bat` 文件

## 🌐 局域网访问

服务器启动后，局域网内的同事可以通过以下方式访问：

1. **自动检测IP**：服务器会自动检测并显示局域网IP
2. **手动配置**：修改客户端的 `server_config.json` 文件
3. **浏览器测试**：直接访问 `http://局域网IP:8002` 测试连通性

## 🛠️ 故障排除

### 问题1：服务器没有自动启动
**解决方案：**
1. 检查启动项设置是否正确
2. 查看Windows事件日志
3. 手动运行启动脚本测试

### 问题2：端口8002被占用
**解决方案：**
1. 使用 `netstat -an | find ":8002"` 检查端口占用
2. 重启电脑释放端口
3. 修改服务器端口（需要同步修改客户端配置）

### 问题3：Python环境问题
**解决方案：**
1. 确保Python已正确安装并添加到PATH
2. 检查依赖包是否安装完整：`pip install -r requirements.txt`
3. 使用完整路径启动Python

## 📝 日志和监控

### 查看服务器日志
1. 运行 `热更新服务器管理.bat`
2. 选择 `[5] 查看服务器日志`

### 监控服务器状态
- 服务器启动后会显示访问地址和端口
- 每次客户端请求都会显示日志信息
- 可通过任务管理器查看Python进程

## 🔒 安全建议

1. **防火墙设置**：确保端口8002在防火墙中允许通过
2. **网络隔离**：建议仅在内部局域网使用
3. **访问控制**：可考虑添加简单的访问验证
4. **定期更新**：定期检查和更新服务器代码

## 📞 技术支持

如果遇到问题，可以：
1. 查看控制台输出的错误信息
2. 检查Windows系统日志
3. 联系技术支持人员

---

**注意**：设置开机启动后，每次开机服务器都会自动运行。如需停止，可使用管理工具或手动结束Python进程。

