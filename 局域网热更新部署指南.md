# 局域网热更新部署指南

## 🚀 服务器端部署

### 1. 启动热更新服务器
```bash
python simple_file_update_server.py
```

服务器启动后会显示：
```
🚀 启动局域网热更新服务器...
📡 服务器地址: http://192.168.1.100:8002
📁 更新文件夹: E:\Xproject-main\updates
...
🌐 局域网访问说明:
   同事需要将客户端配置中的服务器地址改为: http://192.168.1.100:8002
```

### 2. 部署新版本
1. 将新版本exe文件复制到 `updates` 文件夹
2. 文件名建议包含版本号，如：`美术资源上传工具_v1.0.7.exe`
3. 服务器会自动检测并提供给客户端

## 👥 客户端配置

### 方法1：自动检测（推荐）
客户端会自动扫描常见的局域网IP地址，无需手动配置。

### 方法2：配置文件
1. 编辑 `server_config.json` 文件
2. 修改 `manual_lan` 字段为服务器IP：
```json
{
  "endpoints": {
    "manual_lan": "http://192.168.1.100:8002/api"
  }
}
```

### 方法3：代码配置
在 `art_resource_manager.py` 中找到 `update_server_options`，修改IP地址后重新打包。

## 🔧 使用流程

### 服务器管理员
1. 在一台电脑上运行 `python simple_file_update_server.py`
2. 记录显示的IP地址，如：`http://192.168.1.100:8002`
3. 将新版本exe文件放入 `updates` 文件夹
4. 通知同事可以更新

### 同事使用
1. 打开美术资源管理工具
2. 菜单栏 → 帮助 → 检查更新
3. 如果有新版本，点击确认更新
4. 更新完成后应用会自动关闭
5. 手动启动新版本exe文件

## 📁 文件结构

```
项目目录/
├── simple_file_update_server.py    # 服务器程序
├── server_config.json              # 服务器配置
├── 美术资源上传工具.exe            # 主程序
├── 美术资源上传工具_v1.0.6.exe     # v1.0.6版本
└── updates/                         # 更新文件夹
    ├── 美术资源上传工具_v1.0.6.exe # 当前版本
    └── 美术资源上传工具_v1.0.7.exe # 新版本
```

## 🌐 网络要求

- 所有电脑需要在同一局域网内
- 服务器电脑的8002端口需要开放
- 客户端需要能够访问服务器IP的8002端口

## 🔍 故障排除

### 1. 客户端检测不到更新
- 检查服务器是否正常运行
- 确认IP地址配置正确
- 测试网络连通性：`ping 服务器IP`

### 2. 下载失败
- 检查防火墙设置
- 确认8002端口未被占用
- 检查updates文件夹中是否有对应的exe文件

### 3. 更新后还是旧版本
- 确认更新完成后手动启动新版本
- 检查是否启动了正确的exe文件
- 新版本应该包含最新功能（如测试菜单）

## 📝 注意事项

1. **版本管理**：建议使用版本号命名exe文件
2. **文件权限**：确保updates文件夹有读写权限
3. **网络安全**：仅在可信的局域网环境中使用
4. **备份重要**：更新前建议备份当前工作版本


